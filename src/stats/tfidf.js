/**
 * Talisman stats/tfidf
 * =====================
 *
 * Helpers related to TF/IDF computations.
 */
import words from '../tokenizers/words/naive';

// TODO: do we need a name for the document
// TODO: create the handy aliases functions

/**
 * TfIdf class
 *
 * @constructor
 * @param {array} documents - Initial documents.
 */
export class TfIdf {
  constructor(documents) {
    documents = documents || [];

    // Properties
  }

  /**
   * Adding a document to the computation.
   *
   * @param  {mixed} document - The document to add.
   * @return {TfIdf}          - Returns itself for chaining purposes.
   */
  addDocument(document) {

  }
}
